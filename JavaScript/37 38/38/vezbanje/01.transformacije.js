// Inicijalizovati niz `xs` na elemente 20 brojeva iz intervala [10, 40];
function generateArray()
{
    let xs = [];
    // ...
    return xs;
}

// Napraviti niz `ys` koji sadrzi samo parne elemente iz `xs`.
// Obavezno koristiti funkciju `filter`.
xs = generateArray();
console.log('xs: ', xs);
let ys = undefined; // Vas kod
console.log('ys: ', ys);

// Kvadrirati sve elemente u nizu `ys` koristeci funkciju `map`.
ys = undefined; // Vas kod
console.log('ys: ', ys);

// Ispisati elemente iz niza `ys` koristeci `forEach`.
// Vas kod.